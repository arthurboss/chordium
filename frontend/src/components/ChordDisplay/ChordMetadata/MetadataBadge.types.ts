/**
 * Props for the MetadataBadge component
 * Displays a single metadata item with label and value
 */
export interface MetadataBadgeProps {
  /** Label text to display (e.g., "Artist:", "Tuning:") */
  label: string;
  /** Value text to display */
  value: string;
}
